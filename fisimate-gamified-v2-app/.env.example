# Environment variables for fisimate-app.
# The physics chatbot now calls the backend endpoint (chatbot/generate-question)
# instead of Google Gemini, so no AI provider key is required here.

# Base URL of the fisimate-api backend (include trailing slash).
BASE_URL=https://fisimate-api.rafiadipramana.dev/api/v1/
